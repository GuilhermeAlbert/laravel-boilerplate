<?php

namespace App\Services;

use App\Interfaces\Services\StubInterface;
use App\Repositories\StubRepository;

class StubService implements StubInterface
{
    // Protected variable context
    protected $repository;

    /**
     * Constructor method to instantiate a instance
     * @param StubRepository $repository
     */
    public function __construct(StubRepository $repository)
    {
        $this->repository = $repository;
    }
}